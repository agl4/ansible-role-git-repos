---
- hosts: all
  gather_facts: false
  tasks:
    - name: install python
      raw: test -e /usr/bin/python3 || ( apt update && apt install -y python3 )
      changed_when: false
      failed_when: false
    - name: install sudo
      package:
        name:
          - sudo
          - git
